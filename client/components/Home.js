import React, { useState } from 'react';
import { Text, View, StyleSheet, Image, TextInput, TouchableOpacity, ScrollView, LogBox } from 'react-native';
import Register from './Register';
import Login from './Login';

const Home = ({navigation}) => {
    return(
        <View>
            <Text>asjagd</Text>
        </View>
    )
}

export default Home;